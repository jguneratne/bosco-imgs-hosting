import "./style.css";

import {
  defaultGallerySetting,
  // sliderAnimation,
  // highlightCircles,
  clickCircles,
  rightArrowCtrl,
  leftArrowCtrl,
} from "./photoSlider";

console.log("photogallery");

defaultGallerySetting();
// sliderAnimation();
// highlightCircles();
clickCircles();
rightArrowCtrl();
leftArrowCtrl();
